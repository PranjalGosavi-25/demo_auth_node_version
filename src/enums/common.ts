export enum CompanyAccountTypeEnum {
  Supplier = 'Supplier',
  Organization = 'Organization',
  VCP = 'VCP'
}

export enum SupplierTypeEnum {
  Trial = 'Trial'
}

export enum StateEnum {
  Error = 'error',
  Success = 'success',
  Warning = 'warning',
  Gray = 'gray'
}
